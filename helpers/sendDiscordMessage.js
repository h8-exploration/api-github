const { addTask } = require("./bull");

const sendDiscordMessage = () => {
	//  get student data
	// get push data
	//  add data to queue
	console.log(
		"🚀 ~ file: sendDiscordMessage.js ~ line 6 ~ sendDiscordMessage ~ sendDiscordMessage"
	);
};

module.exports = sendDiscordMessage;
